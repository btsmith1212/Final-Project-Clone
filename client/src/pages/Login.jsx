import UserInfo from "../components/UserInfo";

function Login(){
    return(
        <section>
            <UserInfo title={"Login"} page={"Login"} />
        </section>
    )
}

export default Login;